package com.kodewala;

import static org.junit.jupiter.api.Assertions.assertArrayEquals;
import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;

// devevloper will write all the test cases to check the functionality 

public class MultiplynumberTest {
	static Multiplynumber multiplynumber;
	
	@BeforeAll
	static void setUpAll()
	{
		
	}
	
	@Test
	public void testMultiplication()
	{
		Multiplynumber multiplynumber = new Multiplynumber();
		
		int expected = 20;// setting the expected 
		
		int actual = multiplynumber.doMultiplication(2, 10);// based on input what method is returning 
		
		assertEquals(expected, actual); // compare the expected with actual
	}

	
}
